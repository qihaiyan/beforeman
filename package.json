{
  "name": "beforeman",
  "version": "0.0.1",
  "dependencies": {
    "@tauri-apps/api": "^1.0.2"
  },
  "scripts": {
    "tauri": "tauri",
    "dev:client": "yarn tauri dev",
    "build:client": "yarn tauri build",
    "dev:hoppscotch": "cd hoppscotch && pnpm start",
    "build:hoppscotch": "cd hoppscotch && pnpm run generate"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.5",
    "cross-env": "^7.0.3"
  }
}